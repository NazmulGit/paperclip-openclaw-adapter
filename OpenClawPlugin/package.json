{
  "name": "openclaw-paperclip-bridge",
  "version": "0.2.0",
  "description": "OpenClaw ↔ Paperclip bridge plugin (OC side). Registers a paperclip.* tool family so OC agents can comment, close, and create Paperclip issues without raw fetch boilerplate.",
  "type": "module",
  "main": "dist/index.js",
  "files": [
    "dist",
    "openclaw.plugin.json",
    "skills"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  },
  "openclaw": {
    "pluginManifest": "./openclaw.plugin.json"
  }
}
